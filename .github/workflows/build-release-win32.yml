name: Build Win32 (Release)

on:
  workflow_dispatch:

jobs:
  call-workflow-build:
    uses: ./.github/workflows/workflow-build-wtl-submodule.yml
    with:
      platform-arch: Win32
      build-config: Release
