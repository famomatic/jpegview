JPEGView source code readme
***************************

To compile JPEGView you need:

- CMake 3.20 or later (https://cmake.org/download/)
- Visual Studio 2022 (v143 toolset), Community or Professional edition
  (JPEGView 32-bit (x86) and 64-bit (x64) versions are both supported)
- Windows Template Library (WTL), Version 8.1+
  (http://sourceforge.net/projects/wtl/) installed to deps/WTL-sf
  (automatically populated by cloning the submodule: git submodule update --init deps/WTL-sf)


Building from the command line
------------------------------

Configure (x64 Release, for example):

    cmake -B build -G "Visual Studio 17 2022" -A x64
    cmake --build build --config Release

The compiled JPEGView.exe, WICLoader.dll and all required runtime DLLs/config
files are placed under build/out/<config>/ (e.g. build/out/Release/).

To build the 32-bit version, use -A Win32 and a separate build directory:

    cmake -B build32 -G "Visual Studio 17 2022" -A Win32
    cmake --build build32 --config Release

Both Debug and Release configurations are supported.


Building the MSI installer
--------------------------

To also build the WiX MSI installer, enable the JPEGVIEW_BUILD_SETUP option.
This requires WiX Toolset v3.11 or later (https://wixtoolset.org/releases/)
with candle.exe and light.exe available on PATH (or the WIX env var set).

    cmake -B build -G "Visual Studio 17 2022" -A x64 -DJPEGVIEW_BUILD_SETUP=ON
    cmake --build build --config Release --target JPEGView.Setup

The resulting .msi is written to build/msi/.


Please note:
- 64-bit Windows 7 or later is needed to run the 64-bit version.
- The 32-bit version targets modern Windows (XP/Win7-era special-casing has
  been removed along with the VS2017 project files).


NOTE: Visual Studio 2022 is the current active JPEGView development environment.


Legacy notes
************

NOTE: after the tag "last-cpp14-code", there is code that requires C++17.  This
code will not work on previous versions of Visual Studio.  An #ifdef is used
to disable that feature when compiling for standard below C++17.

The old VS2017/VS2019 solution and .vcxproj files have been removed.  If you
still need them, clone the tag v1.2.45.
